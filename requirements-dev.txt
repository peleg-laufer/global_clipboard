# Development and CI dependencies. Not installed in the Docker image.
-r requirements.txt

fastapi-cli==0.0.32   # `fastapi dev` reload server
httpx==0.28.1         # required by fastapi.testclient.TestClient
pytest==9.1.1
ruff==0.16.8
